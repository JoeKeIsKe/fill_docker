/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
        locales: ['fil', 'fit'],
        defaultLocale: 'fil',
        localeDetection: false,
    }
}

module.exports = nextConfig
